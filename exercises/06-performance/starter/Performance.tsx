import { useState, useEffect } from "react";
import styles from "./Performance.module.css";

// TODO: Create an expensive calculation function
function expensiveCalculation(num: number): number {
  // Simulate expensive work
  return num;
}

// TODO: Create a counter component that updates frequently

// TODO: Create an item component (should be optimized)

// TODO: Create a list component (should be optimized)

export default function Performance() {
  // TODO: Implement the performance demo

  return (
    <div className={styles.container}>
      <h2>Performance Optimization</h2>
      {/* Implement UI */}
    </div>
  );
}
